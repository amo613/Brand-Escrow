{
  "name": "pactpay",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous brand↔creator metric-milestone escrow on Algorand (x402). Hackathon root — DB + tooling.",
  "scripts": {
    "db:push": "prisma db push",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio",
    "db:pull": "prisma db pull"
  },
  "devDependencies": {
    "prisma": "^6.2.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@prisma/client": "^6.2.0"
  }
}
